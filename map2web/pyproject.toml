[project]
name = "tmap"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "meilisearch>=0.40.0",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
    "pyarrow>=23.0.1",
]

[tool.fastapi]
entrypoint = "backend.main:app"
