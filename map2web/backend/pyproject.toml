[project]
name = "tmap"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "meilisearch>=0.40.0",
]

[tool.fastapi]
entrypoint = "src.main:app"
