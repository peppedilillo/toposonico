[project]
name = "vec2map"
version = "0.1.0"
description = "Spotify track embeddings UMAP projections."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.0.0",
    "pandas>=2.0.0",
    "pyarrow>=15.0.0",
    "matplotlib>=3.10.8",
]
